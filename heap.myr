pkg heap =
	//This works in single file
	//trait layer @a -> @b :: layer @a =

	trait layer @a -> @b

	trait layer @a -> @b =
		//init : (-> @a)
		//fini : (a : @a -> void)
		get : (a : @a -> @b)
		put : (a : @a, p : @b -> void)
	;;

	trait op @t =
		alloc : (-> @t#)
		free  : (p : @t# -> void)
	;;
;;
